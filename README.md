# fca-wp-mod

Custom firmware for the Comma "White Panda" (or similar STM32F4-based boards) that improves the functionality of Comma for FCA vehicles.

Build with `make`; requires `dfu-util` and Python for installation onto a WP-style board.